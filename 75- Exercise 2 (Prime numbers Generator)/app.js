// Prime Numbers Generator

/** print numbers with condition
 * get no. of of numbers
 * printed numbers = 0
 * current nuber = 2
 * while p.n < no
 *      if c is true
 *          print c
 *          p.n++
 *      c++
 */

/**check if number is prime
 * n
 * prime = true
 * loop i = 2 ===> n-1
 *      if n divided by
 *          not prime
 */

function generatePrimes(numberOfPrimes) {
    let printedNumber = 0
    let currentNumbers = 2
    while(printedNumber < numberOfPrimes) {
        if(isPrime(currenNumber))
    }
}
